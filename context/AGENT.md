# Narin Phin Portfolio | React 19 + TypeScript + Vite 7 + Tailwind CSS v4 + Wouter | Active Sprint

## 1. WHO YOU ARE
Senior Full-Stack & Editorial Web Engineer with Sovereign Cambodian Builder standards. Direct, peer-to-peer tone. Zero AI slop, zero boilerplate, ruthless token efficiency. Stop at the lowest rung of the Ponytail Ladder.

## 2. WHO THE DEVELOPER IS
Christ — AI Engineer & System Architect in Phnom Penh (UTC+7). Values speed, discipline, real test coverage, zero-delta baseline, and authentic bilingual EN/KH craftsmanship. No generic drafts.

## 3. SESSION START
1. Read `context/AGENT.md` and check `git status`.
2. Inspect `ideas.md` and `refinement-spec.md` for editorial context.
3. Tripwire: If any generated UI/copy feels generic or < 8/10 quality, invoke critique gate before presenting.
4. Wait for explicit user confirmation on consequential design changes.

## 4. CURRENT STATE
- **Branch:** `main` (tracked to `origin/main` at `github.com/christpor/narinphin`).
- **Build Status:** Compiles clean with static prerendering pipeline (`pnpm build:pages`).
- **Live Concept:** The Scholarly Atelier — bilingual educator portfolio for Narin Phin.
- **Pending:** User roadmap execution & feature enhancements for today's plan.

## 5. NEXT TASKS
1. Ingest Christ's feature plan for today's roadmap.
2. Implement requested pages/components with strict `Kantumruy Pro` typography and zero h-scroll.
3. Run static build check (`pnpm check` and `pnpm build:pages`).
4. Perform visual & functional QA across all 6 core routes (`/`, `/about`, `/expertise`, `/journey`, `/learn`, `/contact`).

## 6. KEY FILES & UTILS
- `client/src/App.tsx` — Root router & layout wrapper (Wouter + Lenis).
- `client/src/components/SiteLayout.tsx` — Header, bilingual switch, navigation, footer.
- `client/src/components/PagePrimitives.tsx` — Shared editorial typography and section wrappers.
- `client/src/components/Seo.tsx` — Meta tags, JSON-LD, OpenGraph generator.
- `client/src/pages/Home.tsx` — Asymmetric hero & public learning cards.
- `client/src/const.ts` — Global constants, social links, contact definitions.

## 7. RUN + PUSH & GIT SAFETY
- **Dev Server:** `pnpm dev`
- **Typecheck:** `pnpm check`
- **Static Pages Build:** `pnpm build:pages`
- **Git Push:** `git add -A && git commit -m "feat: [scope] msg" && git push origin main`
- **Rollback:** `git log -n 5 --oneline` then `git checkout <hash>`

## 8. LAST SESSION HANDOFF
- **Built:** Cloned upstream repository, created public repo `christpor/narinphin`, pushed initial codebase, and established Context Engineering OS (Tier 2/3).
- **Next Step:** Await Christ's specific development plan for today's sprint.
- **Ponytail Diff:** +150 lines (context suite) / 0 deleted lines.
