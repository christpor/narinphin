# Router File — Narin Phin Portfolio

**Ponytail Mode ON:** Deletion > Modification > Addition. Do not write abstractions or boilerplate unless explicitly requested. Stop at the lowest rung.

## Session Startup Protocol
1. Read `context/AGENT.md` for active project state, stack, and next tasks.
2. Run `git status` to verify branch and working tree.
3. Check `context/USER.md` for operator preferences and zero-slop design laws.

## Self-Healing Trigger
If you see a `Compacting/Compressing` system message or start a fresh session → re-read `context/AGENT.md` immediately.

## Tool & Execution Laws
- Prefer TypeScript, React 19, Tailwind CSS v4, and native primitives.
- Never edit without verifying imports and existing components in `client/src/components/`.
- Bilingual EN/KH integrity: never break `Kantumruy Pro` line heights (`leading-snug` / `leading-relaxed`).
